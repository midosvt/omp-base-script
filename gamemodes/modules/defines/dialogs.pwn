// Defining the dialog IDs with an enum is recommended.
// As you don't have to keep track of used IDs. And you
// should never use literal numbers for IDs - it gets confusing.
enum
{
    DIALOG_NO_RESPONSE,

    DIALOG_REGISTRATION,
    DIALOG_LOGIN
}