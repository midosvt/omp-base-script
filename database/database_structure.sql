CREATE TABLE players (
    ID INT PRIMARY KEY,
    Name VARCHAR(255),
    Hash VARCHAR(255),
    Skin INT,
    Score INT,
    Kills INT,
    Deaths INT,
    x_pos FLOAT,
    y_pos FLOAT,
    z_pos FLOAT,
    angle_pos FLOAT
);
